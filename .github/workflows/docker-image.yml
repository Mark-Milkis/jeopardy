name: Docker Image CI (Legacy)

# This workflow is deprecated. Use build-dev.yml instead.
# Kept for backwards compatibility.

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect Notice
        run: |
          echo "⚠️  This workflow is deprecated."
          echo "Please use build-dev.yml for development builds."
          echo "Use version-release.yml for releases (triggered by git tags)."
          exit 1
